## top-bar

remove user menu in top-right and replace with straight-up logout button in nav bar.

## survey-translations

- Add New button is stretched (padding-block?)fgdoijg

- Need user feedback when duplicating.
- Hide submit button on 'default' (when there
  is nothing to submit)

- do not duplicate the 'current' default option when downloading the existing translations - do not make an editable column.

- when duplicating, the new locale should inherit the existing locale's status (e.g. if you duplicate a 'default' entry, which is ready for use from the start, it should be shown as 'ready for use').

## Registration

- random 500 error on signup; likely to do with ODK Central sync.
-
